#ifndef ESPAC_FIRMWARE_UTILS_H
#define ESPAC_FIRMWARE_UTILS_H

#include <cstdint>
#include <algorithm> // for std::max

uint32_t dim_color(uint32_t color, float percent);

#endif //ESPAC_FIRMWARE_UTILS_H
